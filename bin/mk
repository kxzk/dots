#!/usr/bin/env bash

# create directory and cd into it
mk() {
    if [ $# -eq 0 ]; then
        echo "usage: mk <directory>"
        return 1
    fi

    mkdir -p "$1" && cd "$1"
}

# call the function with all arguments
mk "$@"
